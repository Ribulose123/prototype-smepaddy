
  # Simple Business Dashboard (Copy)

  This is a code bundle for Simple Business Dashboard (Copy). The original project is available at https://www.figma.com/design/hdfoV126SSXcpKHPQ6UmlC/Simple-Business-Dashboard--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  